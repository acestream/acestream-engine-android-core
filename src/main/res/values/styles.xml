<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.NoActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <style name="AppBaseThemeTv" parent="Theme.AppCompat.NoActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="colorPrimary">@color/bluegrey800</item>
        <item name="colorPrimaryDark">@color/bluegrey700</item>
        <item name="colorAccent">@color/main_accent</item>
    </style>

    <style name="AppThemeTv" parent="AppBaseThemeTv">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="MyWebView" parent="Theme.AppCompat.Light">
    </style>

    <style name="MyWebView.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <style name="MyWebViewDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowMinWidthMajor">65%</item>
        <item name="android:windowMinWidthMinor">95%</item>
    </style>

    <style name="AppCompatAlertDialogStyle" parent="Theme.AppCompat.Dialog.Alert">
    </style>

    <style name="Theme.Player" parent="Theme.AppCompat">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="windowActionBarOverlay">true</item>

        <item name="colorPrimary">@color/controls_dark</item>
        <item name="colorPrimaryDark">@color/transparent</item>
        <item name="colorAccent">@color/controls_dark</item>
    </style>

    <style name="Theme.AceStream.Dialog.Light" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/bluegrey800</item>
        <item name="colorPrimaryDark">@color/bluegrey700</item>
        <item name="colorAccent">@color/main_accent</item>
    </style>

    <style name="Theme.AceStream.Dialog.Dark" parent="Theme.AppCompat.Dialog">
        <item name="colorPrimary">@color/bluegrey800</item>
        <item name="colorPrimaryDark">@color/bluegrey700</item>
        <item name="colorAccent">@color/main_accent</item>
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="android:background">@color/background_dark</item>
    </style>

</resources>